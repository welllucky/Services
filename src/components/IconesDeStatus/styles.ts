import styled from "styled-components";

export const IconeDeStatusContainer = styled.section`
	position: fixed;
	display: flex;
	align-items: center;
	flex-direction: column;
	gap: 2rem;
	right: 4rem;
	bottom: 8.2em;
`;
